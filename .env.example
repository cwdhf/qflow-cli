# Qflow CLI 环境变量配置文件
# 将此文件复制为 .env 文件并根据需要修改
# ============================================
# OpenAI 配置 (新添加的集成)
# ============================================

# OpenAI API 密钥 (必需)
# 从 https://platform.openai.com/api-keys 获取
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI API 基础URL (可选)
# 默认: https://api.openai.com/v1
# 可以设置为第三方兼容OpenAI API的服务地址
# 例如: https://api.deepseek.com/v1, http://localhost:8080/v1
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 模型名称 (可选)
# 默认: gpt-3.5-turbo
# 支持的模型: gpt-3.5-turbo, gpt-4, gpt-4-turbo, 或第三方兼容模型
OPENAI_MODEL=gpt-3.5-turbo

# ============================================
# 关于 OPENAI_ORGANIZATION 和 OPENAI_PROJECT 的解释:
# ============================================
# 
# OPENAI_ORGANIZATION (可选):
# - 这是OpenAI企业账户的组织ID
# - 只有当你使用OpenAI企业账户，并且有多个组织时才需要设置
# - 普通个人用户通常不需要这个参数
# - 格式: org-xxxxxxxxxxxxxxxx
# 
# OPENAI_PROJECT (可选):
# - 这是OpenAI企业账户中的项目ID
# - 用于在企业账户中区分不同的项目/团队
# - 普通个人用户通常不需要这个参数
# - 格式: proj_xxxxxxxxxxxxxxxx
# 
# 对于大多数用户来说，这两个参数都不需要设置！
# 只需要设置 OPENAI_API_KEY 就足够了。

# ============================================
# 原有 Gemini/Google 配置
# ============================================

# Gemini API 密钥 (使用Gemini API时必需)
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini 模型名称 (可选)
GEMINI_MODEL=gemini-2.0-flash-exp

# Google Cloud 配置 (使用Vertex AI时必需)
GOOGLE_API_KEY=your-google-api-key-here
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# ============================================
# 通用配置
# ============================================

# 调试模式
DEBUG=false

# 代理设置 (如果需要)
HTTP_PROXY=http://proxy.example.com:8080
HTTPS_PROXY=http://proxy.example.com:8080

# 自定义HTTP头
GEMINI_CLI_CUSTOM_HEADERS=X-My-Header: foo, X-Trace-ID: abc123

# ============================================
# 使用说明:
# ============================================
# 1. 将此文件复制为 .env
#    cp .env.example .env
# 
# 2. 编辑 .env 文件，填入你的实际API密钥
# 
# 3. 启动Gemini CLI:
#    npm run start
# 
# 4. 在认证对话框中选择:
#    - "Use OpenAI API" (使用OpenAI)
#    - "Use Gemini API Key" (使用Gemini)
#    - "Login with Google" (使用Google登录)
# 
# 注意: 环境变量优先级高于配置文件，但 .env 文件中的设置
# 会在启动时自动加载。
